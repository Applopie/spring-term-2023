#include <iostream>
#include <string>

using namespace std;

int main()
{
    std::string s1;
    std::string s2;
    std::cin >> s1 >> s2;
    int a1 = s1.length();
    int a2 = s2.length();
    if (a1 < a2)
    {
        int a3 = a1;
        a1 = a2;
        a2 = a3;
    }
    int a[a1];
    int lenght = 0;
    int t = 0, j = 0;
    while (j < a2)
    {
        while (t != 1)
        {
            if (a)
        }
    }
    cout << lenght << endl;
    return 0;
}